<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jarvis.novel">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme"
        android:name=".core.App"
        android:configChanges="screenSize"
        android:fullBackupContent="@xml/backup_descriptor">
        <activity android:name=".ui.activity.MainActivity"
            android:configChanges="fontScale">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.activity.QRCodeActivity" />

        <activity android:name=".ui.activity.manga.MangaVolumeChapterActivity" />

        <activity android:name=".ui.activity.manga.MangaContentActivity" />

        <activity android:name=".ui.activity.novel.NovelVolumeChapterActivity" />

        <activity android:name=".ui.activity.novel.NovelContentActivity" />
    </application>

</manifest>